let arr1 = ['Nicolas', 'Chanchito', 'Felipe']; // Se declara un arreglo con tres nombres
let mensaje = arr1.join(', '); // Se une el arreglo en una sola cadena, separando los elementos por una coma y un espacio

// console.log(mensaje); // Esta línea está comentada y no se ejecutará, pero imprimiría la cadena resultante de 'mensaje'

let saludo = "Hola mundo! desde NZ"; // Se declara una cadena de texto
let dividido = saludo.split(" "); // Se divide la cadena en un arreglo de palabras, usando el espacio como separador

console.log(dividido.join('-')); // Se une el arreglo de palabras en una sola cadena, separando los elementos por un guion
